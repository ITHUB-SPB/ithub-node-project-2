<!DOCTYPE html>
<html lang="ru">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>новости</title>
    <link href="https://fonts.googleapis.com/css2?family=Source+Sans+Pro&display=swap" rel="stylesheet">

    <style>
           * {
        box-sizing: border-box;
    }

    body {
        font-family: 'Source Sans Pro', sans-serif;
        margin: 0;
        padding: 20px;
        background-color: #dfdfdf;
    }

    .wrapper {
        max-width: 1200px;
        margin: 0 auto;
    }

    .news {
        display: flex;
        flex-direction: column;
        gap: 30px;
    }

    .news-item {
        background: rgb(148, 148, 148);
        padding: 20px;
        border-radius: 8px;
        box-shadow: 0 2px 4px rgba(70, 67, 67, 0.1);
        margin-bottom: 20px;
    }

    .news-item h3 {
        margin: 0 0 10px 0;
        color: #808080;
    }

    .news-item .date {
        color: #383838;
        font-size: 12px;
        margin-bottom: 10px;
    }

    .news-item p {
        color: #979797;
        line-height: 1.5;
        margin-bottom: 15px;
    }

    .news-item a {
        color: #00cca0;
        text-decoration: none;
    }

    .news-item a:hover {
        text-decoration: underline;
    }

    h2 {
        color: #6d6d6d;
        border-bottom: 2px solid #92ff88;
        padding-bottom: 10px;
        margin-bottom: 20px;
    }

    </style>
</head>

<body>
    <header class="header">
        <%- include('./header.partial.ejs') %>
    </header>

    <div class="wrapper">
        <div class="news">
            <% for (const [rubricTitle, newsItems] of Object.entries(context)) { %>
    <h2><%= rubricTitle %></h2>
    <% newsItems.forEach(item => { %>
        <%- include('./newsItem.partial.ejs', { newsItem: item }) %>
    <% }) %>
<% } %>

        </div>
    </div>
</body>

</html>